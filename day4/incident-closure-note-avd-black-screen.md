Resolved. Cause: Graphics stack regression introduced by the updated POOL-FIN-01 image, causing dwm.exe to crash in Intel driver module igdumd64.dll (0xc0000005). Action: New sessions were routed to unaffected POOL-FIN-02, POOL-FIN-01 was restored to a known-good baseline (image rollback and driver alignment), hosts were rebooted, and service was validated before reopening. Preventive: Pin approved GPU driver and AVD agent versions, enforce canary image rollout with logon validation gates, and alert on the Event 1000 (dwm.exe + igdumd64.dll) with Event 9009/Event 40 crash-disconnect pattern. User confirmed working.
